syntax = "proto3";

import "nanopb.proto";

// LoDB `ingest_ttl` row (ramdisk): last successful ingest POST time keyed by node id prefix.
message LotatoIngestTtl {
  bytes pub_key_prefix = 1 [(nanopb).max_size = 4];
  uint32 last_posted_unix = 2;
  uint32 reserved = 3;
}
